<ul class="list-group">
  <% @topics.each do |topic| %>
    <%= link_to topic_path(topic.id) do %>
      <li class="list-group-item">
        <span class="badge">14</span>
        <%= topic.title %>
      </li>
    <% end %>
  <% end %>
</ul>
